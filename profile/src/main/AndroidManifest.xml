<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.fightpandemics.profile">

    <dist:module
        dist:instant="false"
        dist:title="@string/title_profile">
        <dist:delivery>
            <dist:install-time />
        </dist:delivery>

        <dist:fusing dist:include="true" />
    </dist:module>

    <application>
        <activity android:name=".ui.AboutMeActivity"></activity>
        <activity android:name=".ui.SocialMediaLinksActivity" />
        <activity android:name=".ui.ChangeNameActivity" />
        <activity android:name=".ui.profile.MyProfileActivity" />
    </application>

</manifest>